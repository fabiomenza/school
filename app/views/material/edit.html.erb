<%= title "Edit material #{@material.name}" %>


<%= render partial:'material_form', locals: {to_do:'update' , to_met: 'put', what: @material} %>

<%= render 'shared/hide_structural_link' %>
<%= render 'shared/hide_semantic_link' %>