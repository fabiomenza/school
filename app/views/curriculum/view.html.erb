<h1><%= @title %></h1>
<p><%= @description %></p>
<h2>Courses in this curriculum</h2>
<ul>
<% @courses.each do |course| %>
	  <li><%= link_to course.name, course_path(course)  %></li>
<% end %>
</ul>


<%= render "shared/structural_link" %>
<%= render "shared/hide_semantic_link" %>