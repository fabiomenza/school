

<%= form_for what, url:{ controller: 'news', action: to_do}, method: to_met do |f| %>
	<%= f.label :name %> <br>
	<%= f.text_field :name %> <br><br>
	<%= f.label :description%> <br>
	<%= f.text_area :description %> <br>
	<%= f.label :time %><br>
	<%= f.time_select :time %> <br>
    

	<%= f.submit %>
<% end %>