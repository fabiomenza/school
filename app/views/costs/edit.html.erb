<%= render partial:'shared/error_partial', locals:{what: @costs, name:'cost'} %>


<%= form_for @costs do |f| %>
	<p>
		<%= f.label :description %> <br>
		<%= f.text_area :description %>

	</p>
	<p>
		<%= f.submit %>
	</p>

<% end %>