<% if what.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(what.errors.count, "error") %> prohibited
			this <%= name %> from being saved:</h2>
		<ul>
			<% what.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
