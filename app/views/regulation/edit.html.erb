<%= render partial:'shared/error_partial', locals:{what: @regulation, name:'regulation'} %>


<%= form_for @regulation do |f| %>
	<p>
		<%= f.label :description %> <br>
		<%= f.text_area :description %>

	</p>
	<p>
		<%= f.submit %>
	</p>

<% end %>